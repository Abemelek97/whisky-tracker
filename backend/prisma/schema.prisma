generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model User {
  id        String     @id @default(uuid())
  name      String
  phone     String     @unique
  password  String
  role      String     // 'sender', 'receiver', 'superadmin'
  createdAt DateTime   @default(now())
  shipments Shipment[] @relation("UserShipments")
}

model Shipment {
  id               String    @id
  brand            String
  quantity         Int
  sender           String
  senderCity       String    @default("Dubai, UAE")
  receiver         String
  receiverCity     String    @default("Addis Ababa, ET")
  travelerName     String
  travelerPhone    String
  departureDate    String
  status           String    @default("Dispatched")
  receivedQuantity Int?
  notes            String?
  lastCalledAt     String?   // Tracks timestamp when admin dialed the traveler
  createdAt        DateTime  @default(now())
  createdById      String?
  createdBy        User?     @relation("UserShipments", fields: [createdById], references: [id])
}